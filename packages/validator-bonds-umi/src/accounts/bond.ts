/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  HundredthBasisPoint,
  HundredthBasisPointArgs,
  Reserved150,
  Reserved150Args,
  getHundredthBasisPointSerializer,
  getReserved150Serializer,
} from '../types';

/** Bond account for a validator vote address */
export type Bond = Account<BondAccountData>;

export type BondAccountData = {
  discriminator: Array<number>;
  /**
   * Contract root config address. Validator bond is created for this config as PDA
   * but saving the address here for easier access with getProgramAccounts call
   */
  config: PublicKey;
  /**
   * Validator vote address that this bond account is crated for
   * INVARIANTS:
   * - one bond account per validator vote address
   * - bond program does not change received stake account delegation voter_pubkey to any other validator vote
   */
  validatorVoteAccount: PublicKey;
  /**
   * Authority that may close the bond or withdraw stake accounts associated with the bond
   * The same powers has got the owner of the validator vote account
   */
  authority: PublicKey;
  /** Revenue that is distributed from the bond to the protocol */
  revenueShareConfig: HundredthBasisPoint;
  /** PDA Bond address bump seed */
  bump: number;
  /** reserve space for future extensions */
  reserved: Reserved150;
};

export type BondAccountDataArgs = {
  /**
   * Contract root config address. Validator bond is created for this config as PDA
   * but saving the address here for easier access with getProgramAccounts call
   */
  config: PublicKey;
  /**
   * Validator vote address that this bond account is crated for
   * INVARIANTS:
   * - one bond account per validator vote address
   * - bond program does not change received stake account delegation voter_pubkey to any other validator vote
   */
  validatorVoteAccount: PublicKey;
  /**
   * Authority that may close the bond or withdraw stake accounts associated with the bond
   * The same powers has got the owner of the validator vote account
   */
  authority: PublicKey;
  /** Revenue that is distributed from the bond to the protocol */
  revenueShareConfig: HundredthBasisPointArgs;
  /** PDA Bond address bump seed */
  bump: number;
  /** reserve space for future extensions */
  reserved: Reserved150Args;
};

export function getBondAccountDataSerializer(): Serializer<
  BondAccountDataArgs,
  BondAccountData
> {
  return mapSerializer<BondAccountDataArgs, any, BondAccountData>(
    struct<BondAccountData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['config', publicKeySerializer()],
        ['validatorVoteAccount', publicKeySerializer()],
        ['authority', publicKeySerializer()],
        ['revenueShareConfig', getHundredthBasisPointSerializer()],
        ['bump', u8()],
        ['reserved', getReserved150Serializer()],
      ],
      { description: 'BondAccountData' }
    ),
    (value) => ({
      ...value,
      discriminator: [224, 128, 48, 251, 182, 246, 111, 196],
    })
  ) as Serializer<BondAccountDataArgs, BondAccountData>;
}

export function deserializeBond(rawAccount: RpcAccount): Bond {
  return deserializeAccount(rawAccount, getBondAccountDataSerializer());
}

export async function fetchBond(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<Bond> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'Bond');
  return deserializeBond(maybeAccount);
}

export async function safeFetchBond(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<Bond | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializeBond(maybeAccount) : null;
}

export async function fetchAllBond(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<Bond[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'Bond');
    return deserializeBond(maybeAccount);
  });
}

export async function safeFetchAllBond(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<Bond[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) => deserializeBond(maybeAccount as RpcAccount));
}

export function getBondGpaBuilder(context: Pick<Context, 'rpc' | 'programs'>) {
  const programId = context.programs.getPublicKey(
    'validatorBonds',
    'vbondsKbsC4QSLQQnn6ngZvkqfywn6KgEeQbkGSpk1V'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      discriminator: Array<number>;
      config: PublicKey;
      validatorVoteAccount: PublicKey;
      authority: PublicKey;
      revenueShareConfig: HundredthBasisPointArgs;
      bump: number;
      reserved: Reserved150Args;
    }>({
      discriminator: [0, array(u8(), { size: 8 })],
      config: [8, publicKeySerializer()],
      validatorVoteAccount: [40, publicKeySerializer()],
      authority: [72, publicKeySerializer()],
      revenueShareConfig: [104, getHundredthBasisPointSerializer()],
      bump: [108, u8()],
      reserved: [109, getReserved150Serializer()],
    })
    .deserializeUsing<Bond>((account) => deserializeBond(account))
    .whereField('discriminator', [224, 128, 48, 251, 182, 246, 111, 196]);
}

export function getBondSize(): number {
  return 251;
}
