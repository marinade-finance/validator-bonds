---
summary: |
  Validator Bonds eval harness + skill context files. Main focus: skill quality + eval correctness.
  - open: PR for 20260604_analytics not yet opened
  - open: 5 eval cases fail exit code 1 (claude auth expired — run `claude login` then re-run)
  - open: institutional-staking eval cases + calc questions not yet written
  - [resolved 2026-06-16: skill adjacent .md files — sam-blacklist.md, institutional-staking.md written; ds-scoring.md dropped (legacy)]
  - [resolved 2026-06-16: remaining 4 skills linked into .claude/skills; all skill frontmatters fixed by wisdom subagent]
  - [resolved 2026-06-16: add calculation questions — done: calc-bid-penalty, calc-bond-balance, calc-bid-trigger, calc-bidding-reward]
  - [resolved 2026-06-16: commission-increase-penalty — facts changed to actual_non_bid_pmpe + code identifiers]
  - [resolved 2026-06-16: bond-sizing wrong_fact "cap is reduced" — replaced with "always revoked"]
  - [resolved 2026-06-16: bond-risk-fee — bond_risk_fee_sol → bondRiskFeeSol (camelCase, public ds-sam)]
  - [resolved 2026-06-16: blacklist slow-slots — hyphen fix + sam-blacklist.md adjacent context written]
---

## 08:15

Fixed two flaky eval cases: `bid-too-low-penalty` (fact "decreased" was chasing
nondeterministic prose — swapped to code ids `previous_bidPmpe`, `calculations.ts`)
and `bidding-settlement` ("Marinade stakers" vs "mSOL holders" — swapped to
`active_delegation_lamports`). Root cause: facts grounded in prose synonyms will
always flap; code identifiers from source are deterministic.

## 09:10

Added 6 new eval cases (calc-bid-penalty, calc-bond-balance, calc-bid-trigger,
calc-bidding-reward, auction-ranking, reward-sources) with exact numeric facts.
Strengthened 4 existing (bond-risk-fee, commission-increase, last-price-clearing,
psr-definition) with code identifiers. Pattern: calculation facts must include
expected numeric result, not just code term presence.

## 09:45

Eval harness bugs fixed: Bun `$` has no `.timeout()` method (removed); `pnpm eval --`
passes literal `--` to parseArgs treating flags as positionals (stripped). Plugin now
auto-loads via package.json `--plugin-dir plugins/validator-bonds`. Skill epoch rule
added: never hardcode epoch numbers, query API. Private repo catalog added to SKILL.md
(ds-scoring, institutional-staking, sam-blacklist) with access notes.

## 10:05

Ran 5 risky cases. Results: blacklist-penalty ✓; bond-risk-fee ✗ (BondRiskFeeDetails
not cited — fact changed to calcBondRiskFee, trigger is in ds-sam not ds-scoring);
institutional-payout ✗ (answer correct but missed "50bps APY" — answer describes payout
structure from private institutional-staking repo, no public source); bond-sizing ✗
(wrong_fact "cap is reduced" fires — answer does say cap is reduced, wrong_fact logic
needs review); commission-increase-penalty ✗ (all facts miss — answer describes trigger
formula from collect_commission_increase_events but not in terms skill expects).

## 11:30

Fixed 5 cases post-analysis. Root cause: prose facts flap; code identifiers don't.
Key fixes: bondRiskFeeSol (camelCase from public ds-sam, not snake_case from private
ds-scoring); commission-increase now uses actual_non_bid_pmpe + before_sam_commission_increase_pmpe
(formula terms the model reliably cites); bond-sizing wrong_fact "cap is reduced" →
"always revoked" (answer correctly says cap IS reduced); slow-slots hyphen fix (model
says "slow-slots", fact was "slow slots"). SKILL.md: removed epoch frequency claim,
added InstitutionalPayout/CommissionSamIncrease detail paragraphs, fixed BondRiskFee
row. Added facts/sam-blacklist-mechanism.md from .refs/sam-blacklist scripts.
eval.ts: bare case names now resolve against defaultCasesDir. package.json: duplicate
yaml dep removed.

## 14:20

Wrote skill adjacent context files: sam-blacklist.md (public policy only — sandwich

> 30% rate, slow-slot consecutive epochs), institutional-staking.md (Select program
> meaning: 24 validators, 50bps APY guarantee, bond-backed shortfall, public API at
> institutional-staking.marinade.finance). ds-scoring.md dropped — user confirmed it's
> legacy data-shuffler. SKILL.md ds-scoring entry demoted to one-liner. 5 auth-failed
> eval cases blocked on `claude login`.
