/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  TransactionBuilder,
  publicKey,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type CloseSettlementClaimInstructionAccounts = {
  settlement: PublicKey | Pda;
  settlementClaim: PublicKey | Pda;
  rentCollector?: PublicKey | Pda;
};

// Data.
export type CloseSettlementClaimInstructionData = {
  discriminator: Array<number>;
};

export type CloseSettlementClaimInstructionDataArgs = {};

export function getCloseSettlementClaimInstructionDataSerializer(): Serializer<
  CloseSettlementClaimInstructionDataArgs,
  CloseSettlementClaimInstructionData
> {
  return mapSerializer<
    CloseSettlementClaimInstructionDataArgs,
    any,
    CloseSettlementClaimInstructionData
  >(
    struct<CloseSettlementClaimInstructionData>(
      [['discriminator', array(u8(), { size: 8 })]],
      { description: 'CloseSettlementClaimInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [103, 26, 247, 104, 254, 134, 218, 94],
    })
  ) as Serializer<
    CloseSettlementClaimInstructionDataArgs,
    CloseSettlementClaimInstructionData
  >;
}

// Instruction.
export function closeSettlementClaim(
  context: Pick<Context, 'programs'>,
  input: CloseSettlementClaimInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'validatorBonds',
    'vbondsKbsC4QSLQQnn6ngZvkqfywn6KgEeQbkGSpk1V'
  );

  // Accounts.
  const resolvedAccounts = {
    settlement: {
      index: 0,
      isWritable: false as boolean,
      value: input.settlement ?? null,
    },
    settlementClaim: {
      index: 1,
      isWritable: true as boolean,
      value: input.settlementClaim ?? null,
    },
    rentCollector: {
      index: 2,
      isWritable: true as boolean,
      value: input.rentCollector ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.rentCollector.value) {
    resolvedAccounts.rentCollector.value = publicKey(
      'SysvarRent111111111111111111111111111111111'
    );
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getCloseSettlementClaimInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
