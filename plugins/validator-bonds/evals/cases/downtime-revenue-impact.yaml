question: >
  `DowntimeRevenueImpact` is a `ProtectedEvent` sub-kind in the `SettlementReason`
  enum. Explain what triggers it and how the payout is split between the validator
  bond and Marinade, including which loss band each covers.
facts:
  - 'DowntimeRevenueImpact'
  - 'ValidatorBond'
  - 'ProtectedEvent'
  - 'covered_range_bps'
wrong_facts:
  - 'bond covers all'
  - 'validator covers 100%'
  - 'bond covers 100'
