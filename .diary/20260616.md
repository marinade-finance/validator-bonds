---
summary: |
  Validator Bonds eval harness + skill context files. Main focus: skill quality + eval correctness.
  - open: PR for 20260604_analytics not yet opened
  - open: 5 eval cases fail exit code 1 — `claude login` then: pnpm eval --model opus psr-definition reward-sources settlement-types two-staker-populations withdrawal-steps
  - [resolved 2026-06-16: institutional-staking eval cases — 4 cases written (validator-list, payout-calc, bond-limit, payout-sources)]
  - [resolved 2026-06-16: skill adjacent .md files — sam-blacklist.md, institutional-staking.md written; ds-scoring.md dropped (legacy)]
  - [resolved 2026-06-16: remaining 4 skills linked into .claude/skills; all skill frontmatters fixed by wisdom subagent]
  - [resolved 2026-06-16: add calculation questions — done: calc-bid-penalty, calc-bond-balance, calc-bid-trigger, calc-bidding-reward]
  - [resolved 2026-06-16: commission-increase-penalty — facts changed to actual_non_bid_pmpe + code identifiers]
  - [resolved 2026-06-16: bond-sizing wrong_fact "cap is reduced" — replaced with "always revoked"]
  - [resolved 2026-06-16: bond-risk-fee — bond_risk_fee_sol → bondRiskFeeSol (camelCase, public ds-sam)]
  - [resolved 2026-06-16: blacklist slow-slots — hyphen fix + sam-blacklist.md adjacent context written]
  - [resolved 2026-06-16: deep QA pass — institutional-payout-calc rewritten (was fixed-50bps, contradicted percentile floor); PSR dashboard URL fixed (validator-bonds.marinade.finance NXDOMAIN → psr.marinade.finance); claiming window + institutional fees grounded as config-set]
---

## 08:15

Fixed two flaky eval cases: `bid-too-low-penalty` (fact "decreased" was chasing
nondeterministic prose — swapped to code ids `previous_bidPmpe`, `calculations.ts`)
and `bidding-settlement` ("Marinade stakers" vs "mSOL holders" — swapped to
`active_delegation_lamports`). Root cause: facts grounded in prose synonyms will
always flap; code identifiers from source are deterministic.

## 09:10

Added 6 new eval cases (calc-bid-penalty, calc-bond-balance, calc-bid-trigger,
calc-bidding-reward, auction-ranking, reward-sources) with exact numeric facts.
Strengthened 4 existing (bond-risk-fee, commission-increase, last-price-clearing,
psr-definition) with code identifiers. Pattern: calculation facts must include
expected numeric result, not just code term presence.

## 09:45

Eval harness bugs fixed: Bun `$` has no `.timeout()` method (removed); `pnpm eval --`
passes literal `--` to parseArgs treating flags as positionals (stripped). Plugin now
auto-loads via package.json `--plugin-dir plugins/validator-bonds`. Skill epoch rule
added: never hardcode epoch numbers, query API. Private repo catalog added to SKILL.md
(ds-scoring, institutional-staking, sam-blacklist) with access notes.

## 10:05

Ran 5 risky cases. Results: blacklist-penalty ✓; bond-risk-fee ✗ (BondRiskFeeDetails
not cited — fact changed to calcBondRiskFee, trigger is in ds-sam not ds-scoring);
institutional-payout ✗ (answer correct but missed "50bps APY" — answer describes payout
structure from private institutional-staking repo, no public source); bond-sizing ✗
(wrong_fact "cap is reduced" fires — answer does say cap is reduced, wrong_fact logic
needs review); commission-increase-penalty ✗ (all facts miss — answer describes trigger
formula from collect_commission_increase_events but not in terms skill expects).

## 11:30

Fixed 5 cases post-analysis. Root cause: prose facts flap; code identifiers don't.
Key fixes: bondRiskFeeSol (camelCase from public ds-sam, not snake_case from private
ds-scoring); commission-increase now uses actual_non_bid_pmpe + before_sam_commission_increase_pmpe
(formula terms the model reliably cites); bond-sizing wrong_fact "cap is reduced" →
"always revoked" (answer correctly says cap IS reduced); slow-slots hyphen fix (model
says "slow-slots", fact was "slow slots"). SKILL.md: removed epoch frequency claim,
added InstitutionalPayout/CommissionSamIncrease detail paragraphs, fixed BondRiskFee
row. Added facts/sam-blacklist-mechanism.md from .refs/sam-blacklist scripts.
eval.ts: bare case names now resolve against defaultCasesDir. package.json: duplicate
yaml dep removed.

## 14:20

Wrote skill adjacent context files: sam-blacklist.md (public policy only — sandwich

> 30% rate, slow-slot consecutive epochs), institutional-staking.md (Select program
> meaning: 24 validators, PSR-percentile APY floor via psrPercentileApy, bond-backed
> shortfall, public API + /docs-json OpenAPI spec). ds-scoring.md dropped — legacy
> data-shuffler. SKILL.md ds-scoring entry demoted to one-liner. 5 auth-failed eval
> cases blocked on `claude login`.

## 16:15

find skill rewritten: absorbed recall-memories (facts lookup + research in one skill);
removed duplicate lookup tables in favor of pointers to marinade-sam-bond/marinade-docs/
marinade-ecosystem; recall-memories deleted (symlink + SKILL.md). ds-scoring/BondRiskFee
false claim fixed in marinade-docs + marinade-ecosystem. "50bps APY guarantee" corrected
to PSR-percentile APY floor (psrPercentileApy — verified against .refs/institutional-staking
code: uses psrPercentile=50 of network validator APYs, not a fixed 0.5%). 25 ecosystem +
4 institutional eval cases added (54 total). two-staker-populations fact 50bps → PSR.

## 17:40

Deep QA pass on the validator-bonds plugin (skills + 68 eval cases), all
cross-checked against settlement-distributions/ source, settlement-config.yaml,
the on-chain program, and live APIs.

**Critical eval fix — institutional-payout-calc:** the case premised a fixed
50bps (0.5%) APY floor and demanded 50bps/0.0137/13699 as facts. This directly
contradicts the corrected skill (PSR-percentile floor, psr_percentile_apy = 50th
percentile of network APY) and no public code computes per-staker SOL anyway
(payout_stakers/payout_distributors are pre-computed upstream in the private
repo). Rewrote to test the actual mechanism + public identifiers; 50bps/fixed now
wrong_facts.

**Skill bug — dead URL:** marinade-docs listed the PSR dashboard as
validator-bonds.marinade.finance, which is NXDOMAIN (curl). Correct host is
psr.marinade.finance (200, matches marinade-ecosystem). Also added the
institutional-staking (Select) API base + routes to the Live APIs table and
when_to_use.

**Grounding configurable values (same class as the earlier withdraw_lockup fix):**

- claiming window: SKILL.md "~4 epochs" → config.epochs_to_claim_settlement +
  the close_settlement.rs expiry condition; eval swapped fragile ~4 epochs fact
  for the identifier, added fixed-4-epoch-window wrong_fact.
- institutional fees: validator_fee_bps/distributor_fee_bps marked config-set
  (current 10/30bps, read /v1/configs/latest) — field names verified in public
  institutional-distribution source.
- BondRiskFee formula: exposedStake → value (forced-undelegation SOL), matching
  the calc evals; psrPercentileApy (camel, private) → psr_percentile_apy (snake,
  public-verified).

**Eval strengthening:** reward-sources += block_commission_claim (4th
ResultSettlementClaims component); bond-authority += check_bond_authority
(checks.rs:109, question demands a code citation); ecosystem
validator-scoring/blacklist/cli-npm disambiguated to remove false-negative risk
(ds-sam vs delegation-strategy-2, etc.).

Verified all calc-case arithmetic against source (bid penalty 200/184/16,
blacklist 200, commission markup 0.022/0.04, downtime clamp, bond-risk-fee
0.5 SOL). Live eval blocked on `claude login` (no creds in this env, known
blocker) — list-mode confirms all edited cases parse. eval.ts left untouched
(pre-existing in-flight stderr-logging change, not mine).
