question: >
  Using the CommissionSamIncrease markup branch in
  settlement-distributions/settlement-common/src/protected_events.rs and the
  config in settlement-config.yaml:

  A validator raised commission so that expected_epr = 0.10 and actual_epr =
  0.08. This time actual_inflation_commission = 9%. The settlement covers
  stake = 10,000 SOL.

  Compute: (a) base_cps, (b) which markup applies and its fraction, (c)
  claim_per_stake, (d) the payout in SOL. Show each formula step.
facts:
  - '0.02'
  - 'penalty_markup_bps'
  - '0.04'
  - '400 SOL'
  - 'extra_penalty_threshold_bps'
wrong_facts:
  - '220 SOL'
  - '200 SOL'
