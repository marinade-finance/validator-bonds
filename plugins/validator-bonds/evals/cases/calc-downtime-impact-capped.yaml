question: >
  Using the DowntimeRevenueImpact loss-range clamp in
  settlement-distributions/settlement-common/src/protected_events.rs
  (claim_amount_in_loss_range) and the ValidatorBond-funded config in
  settlement-config.yaml:

  A validator with expected_epr = 0.08 suffered a severe outage: actual_credits
  = 400,000 against expected_credits = 1,000,000. The settlement covers stake =
  10,000 SOL.

  Compute: (a) the raw claim_per_stake, (b) the max_claim_per_stake,
  (c) the clamped claim_per_stake, (d) the bond-funded payout in SOL. Show each
  formula step.
facts:
  - '0.048'
  - '0.04'
  - '400 SOL'
  - 'covered_range_bps'
  - 'Marinade'
wrong_facts:
  - '480 SOL'
  - 'bond covers all'
  - 'validator covers 100%'
