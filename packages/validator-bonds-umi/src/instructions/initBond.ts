/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  publicKey as publicKeySerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  HundredthBasisPoint,
  HundredthBasisPointArgs,
  getHundredthBasisPointSerializer,
} from '../types';

// Accounts.
export type InitBondInstructionAccounts = {
  /** the config root account under which the bond is created */
  config: PublicKey | Pda;
  validatorVoteAccount: PublicKey | Pda;
  /** only the owner authority of the validator vote account can create the bond */
  authority?: Signer;
  bond: PublicKey | Pda;
  /** rent exempt payer of validator bond account creation */
  rentPayer?: Signer;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type InitBondInstructionData = {
  bondAuthority: PublicKey;
  revenueShareConfig: HundredthBasisPoint;
};

export type InitBondInstructionDataArgs = {
  bondAuthority: PublicKey;
  revenueShareConfig: HundredthBasisPointArgs;
};

export function getInitBondInstructionDataSerializer(): Serializer<
  InitBondInstructionDataArgs,
  InitBondInstructionData
> {
  return struct<InitBondInstructionData>(
    [
      ['bondAuthority', publicKeySerializer()],
      ['revenueShareConfig', getHundredthBasisPointSerializer()],
    ],
    { description: 'InitBondInstructionData' }
  ) as Serializer<InitBondInstructionDataArgs, InitBondInstructionData>;
}

// Args.
export type InitBondInstructionArgs = InitBondInstructionDataArgs;

// Instruction.
export function initBond(
  context: Pick<Context, 'identity' | 'payer' | 'programs'>,
  input: InitBondInstructionAccounts & InitBondInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'validatorBonds',
    'vbondsKbsC4QSLQQnn6ngZvkqfywn6KgEeQbkGSpk1V'
  );

  // Accounts.
  const resolvedAccounts = {
    config: {
      index: 0,
      isWritable: false as boolean,
      value: input.config ?? null,
    },
    validatorVoteAccount: {
      index: 1,
      isWritable: false as boolean,
      value: input.validatorVoteAccount ?? null,
    },
    authority: {
      index: 2,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    bond: { index: 3, isWritable: true as boolean, value: input.bond ?? null },
    rentPayer: {
      index: 4,
      isWritable: true as boolean,
      value: input.rentPayer ?? null,
    },
    systemProgram: {
      index: 5,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitBondInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.rentPayer.value) {
    resolvedAccounts.rentPayer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getInitBondInstructionDataSerializer().serialize(
    resolvedArgs as InitBondInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
