/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type InitSettlementInstructionAccounts = {
  config: PublicKey | Pda;
  bond: PublicKey | Pda;
  settlement: PublicKey | Pda;
  /** operator signer authority that is allowed to create the settlement account */
  operatorAuthority: Signer;
  /** rent exempt payer of account creation */
  rentPayer?: Signer;
  clock: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type InitSettlementInstructionData = {
  discriminator: Array<number>;
  merkleRoot: Uint8Array;
  voteAccount: PublicKey;
  settlementTotalClaim: bigint;
  settlementNumNodes: bigint;
  rentCollector: PublicKey;
};

export type InitSettlementInstructionDataArgs = {
  merkleRoot: Uint8Array;
  voteAccount: PublicKey;
  settlementTotalClaim: number | bigint;
  settlementNumNodes: number | bigint;
  rentCollector: PublicKey;
};

export function getInitSettlementInstructionDataSerializer(): Serializer<
  InitSettlementInstructionDataArgs,
  InitSettlementInstructionData
> {
  return mapSerializer<
    InitSettlementInstructionDataArgs,
    any,
    InitSettlementInstructionData
  >(
    struct<InitSettlementInstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['merkleRoot', bytes({ size: 32 })],
        ['voteAccount', publicKeySerializer()],
        ['settlementTotalClaim', u64()],
        ['settlementNumNodes', u64()],
        ['rentCollector', publicKeySerializer()],
      ],
      { description: 'InitSettlementInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [152, 178, 0, 65, 52, 210, 247, 58],
    })
  ) as Serializer<
    InitSettlementInstructionDataArgs,
    InitSettlementInstructionData
  >;
}

// Args.
export type InitSettlementInstructionArgs = InitSettlementInstructionDataArgs;

// Instruction.
export function initSettlement(
  context: Pick<Context, 'payer' | 'programs'>,
  input: InitSettlementInstructionAccounts & InitSettlementInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'validatorBonds',
    'vbondsKbsC4QSLQQnn6ngZvkqfywn6KgEeQbkGSpk1V'
  );

  // Accounts.
  const resolvedAccounts = {
    config: {
      index: 0,
      isWritable: false as boolean,
      value: input.config ?? null,
    },
    bond: { index: 1, isWritable: false as boolean, value: input.bond ?? null },
    settlement: {
      index: 2,
      isWritable: true as boolean,
      value: input.settlement ?? null,
    },
    operatorAuthority: {
      index: 3,
      isWritable: false as boolean,
      value: input.operatorAuthority ?? null,
    },
    rentPayer: {
      index: 4,
      isWritable: true as boolean,
      value: input.rentPayer ?? null,
    },
    clock: {
      index: 5,
      isWritable: false as boolean,
      value: input.clock ?? null,
    },
    systemProgram: {
      index: 6,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitSettlementInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.rentPayer.value) {
    resolvedAccounts.rentPayer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getInitSettlementInstructionDataSerializer().serialize(
    resolvedArgs as InitSettlementInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
