/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  Reserved150,
  Reserved150Args,
  getReserved150Serializer,
} from '../types';

/**
 * Settlement claim serves for deduplication purposes to not allow
 * claiming the same settlement with the same claiming data twice.
 */

export type SettlementClaim = Account<SettlementClaimAccountData>;

export type SettlementClaimAccountData = {
  discriminator: Array<number>;
  /** settlement account this claim belongs under */
  settlement: PublicKey;
  /** stake authority as part of the merkle proof for this claim */
  stakeAuthority: PublicKey;
  /** withdraw authority that has got permission to withdraw the claim */
  withdrawAuthority: PublicKey;
  /** vote account as part of the merkle proof for this claim */
  voteAccount: PublicKey;
  /** claim amount */
  claim: bigint;
  /** PDA account bump, one claim per settlement */
  bump: number;
  /** rent collector account to get the rent back for claim account creation */
  rentCollector: PublicKey;
  /** reserve space for future extensions */
  reserved: Reserved150;
};

export type SettlementClaimAccountDataArgs = {
  /** settlement account this claim belongs under */
  settlement: PublicKey;
  /** stake authority as part of the merkle proof for this claim */
  stakeAuthority: PublicKey;
  /** withdraw authority that has got permission to withdraw the claim */
  withdrawAuthority: PublicKey;
  /** vote account as part of the merkle proof for this claim */
  voteAccount: PublicKey;
  /** claim amount */
  claim: number | bigint;
  /** PDA account bump, one claim per settlement */
  bump: number;
  /** rent collector account to get the rent back for claim account creation */
  rentCollector: PublicKey;
  /** reserve space for future extensions */
  reserved: Reserved150Args;
};

export function getSettlementClaimAccountDataSerializer(): Serializer<
  SettlementClaimAccountDataArgs,
  SettlementClaimAccountData
> {
  return mapSerializer<
    SettlementClaimAccountDataArgs,
    any,
    SettlementClaimAccountData
  >(
    struct<SettlementClaimAccountData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['settlement', publicKeySerializer()],
        ['stakeAuthority', publicKeySerializer()],
        ['withdrawAuthority', publicKeySerializer()],
        ['voteAccount', publicKeySerializer()],
        ['claim', u64()],
        ['bump', u8()],
        ['rentCollector', publicKeySerializer()],
        ['reserved', getReserved150Serializer()],
      ],
      { description: 'SettlementClaimAccountData' }
    ),
    (value) => ({
      ...value,
      discriminator: [216, 103, 231, 246, 171, 99, 124, 133],
    })
  ) as Serializer<SettlementClaimAccountDataArgs, SettlementClaimAccountData>;
}

export function deserializeSettlementClaim(
  rawAccount: RpcAccount
): SettlementClaim {
  return deserializeAccount(
    rawAccount,
    getSettlementClaimAccountDataSerializer()
  );
}

export async function fetchSettlementClaim(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<SettlementClaim> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'SettlementClaim');
  return deserializeSettlementClaim(maybeAccount);
}

export async function safeFetchSettlementClaim(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<SettlementClaim | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializeSettlementClaim(maybeAccount) : null;
}

export async function fetchAllSettlementClaim(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<SettlementClaim[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'SettlementClaim');
    return deserializeSettlementClaim(maybeAccount);
  });
}

export async function safeFetchAllSettlementClaim(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<SettlementClaim[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializeSettlementClaim(maybeAccount as RpcAccount)
    );
}

export function getSettlementClaimGpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'validatorBonds',
    'vbondsKbsC4QSLQQnn6ngZvkqfywn6KgEeQbkGSpk1V'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      discriminator: Array<number>;
      settlement: PublicKey;
      stakeAuthority: PublicKey;
      withdrawAuthority: PublicKey;
      voteAccount: PublicKey;
      claim: number | bigint;
      bump: number;
      rentCollector: PublicKey;
      reserved: Reserved150Args;
    }>({
      discriminator: [0, array(u8(), { size: 8 })],
      settlement: [8, publicKeySerializer()],
      stakeAuthority: [40, publicKeySerializer()],
      withdrawAuthority: [72, publicKeySerializer()],
      voteAccount: [104, publicKeySerializer()],
      claim: [136, u64()],
      bump: [144, u8()],
      rentCollector: [145, publicKeySerializer()],
      reserved: [177, getReserved150Serializer()],
    })
    .deserializeUsing<SettlementClaim>((account) =>
      deserializeSettlementClaim(account)
    )
    .whereField('discriminator', [216, 103, 231, 246, 171, 99, 124, 133]);
}

export function getSettlementClaimSize(): number {
  return 319;
}
