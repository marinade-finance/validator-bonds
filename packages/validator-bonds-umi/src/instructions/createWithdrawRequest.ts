/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  publicKey,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import { Serializer, struct, u64 } from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type CreateWithdrawRequestInstructionAccounts = {
  /** the config root account under which the bond was created */
  config: PublicKey | Pda;
  bond: PublicKey | Pda;
  validatorVoteAccount: PublicKey | Pda;
  /** validator vote account owner or bond authority may ask for the withdrawal */
  authority?: Signer;
  withdrawRequest: PublicKey | Pda;
  /** rent exempt payer of withdraw request account creation */
  rentPayer?: Signer;
  systemProgram?: PublicKey | Pda;
  clock: PublicKey | Pda;
};

// Data.
export type CreateWithdrawRequestInstructionData = { amount: bigint };

export type CreateWithdrawRequestInstructionDataArgs = {
  amount: number | bigint;
};

export function getCreateWithdrawRequestInstructionDataSerializer(): Serializer<
  CreateWithdrawRequestInstructionDataArgs,
  CreateWithdrawRequestInstructionData
> {
  return struct<CreateWithdrawRequestInstructionData>([['amount', u64()]], {
    description: 'CreateWithdrawRequestInstructionData',
  }) as Serializer<
    CreateWithdrawRequestInstructionDataArgs,
    CreateWithdrawRequestInstructionData
  >;
}

// Args.
export type CreateWithdrawRequestInstructionArgs =
  CreateWithdrawRequestInstructionDataArgs;

// Instruction.
export function createWithdrawRequest(
  context: Pick<Context, 'identity' | 'programs'>,
  input: CreateWithdrawRequestInstructionAccounts &
    CreateWithdrawRequestInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'validatorBonds',
    'vbondsKbsC4QSLQQnn6ngZvkqfywn6KgEeQbkGSpk1V'
  );

  // Accounts.
  const resolvedAccounts = {
    config: {
      index: 0,
      isWritable: false as boolean,
      value: input.config ?? null,
    },
    bond: { index: 1, isWritable: false as boolean, value: input.bond ?? null },
    validatorVoteAccount: {
      index: 2,
      isWritable: false as boolean,
      value: input.validatorVoteAccount ?? null,
    },
    authority: {
      index: 3,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    withdrawRequest: {
      index: 4,
      isWritable: true as boolean,
      value: input.withdrawRequest ?? null,
    },
    rentPayer: {
      index: 5,
      isWritable: true as boolean,
      value: input.rentPayer ?? null,
    },
    systemProgram: {
      index: 6,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
    clock: {
      index: 7,
      isWritable: false as boolean,
      value: input.clock ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: CreateWithdrawRequestInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getCreateWithdrawRequestInstructionDataSerializer().serialize(
    resolvedArgs as CreateWithdrawRequestInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
